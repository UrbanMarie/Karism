user:
    resource: "@UserBundle/Controller/"
    type:     annotation
    prefix:   /user

admin:
    resource: "@AdminBundle/Controller/"
    type:     annotation
    prefix:   /admin

app:
    resource: '@AppBundle/Controller/'
    type: annotation
